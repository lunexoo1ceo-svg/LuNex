<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>LuNex AI</title>
<style>
  body {
    margin: 0;
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    background: #0b0f2c;
    color: #fff;
    display: flex;
    flex-direction: column;
    align-items: center;
    height: 100vh;
  }

  header {
    text-align: center;
    margin: 20px 0;
  }

  header img {
    width: 150px;
  }

  header h1 {
    font-size: 2.5rem;
    margin: 10px 0 0;
    background: linear-gradient(90deg, #00f0ff, #9b4cff);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  .chat-container {
    flex: 1;
    width: 90%;
    max-width: 600px;
    display: flex;
    flex-direction: column;
    justify-content: flex-end;
    margin-bottom: 20px;
    border: 1px solid #444;
    border-radius: 10px;
    padding: 10px;
    background: rgba(0,0,0,0.5);
    overflow-y: auto;
  }

  .chat-message {
    margin: 5px 0;
    padding: 8px 12px;
    border-radius: 10px;
    max-width: 80%;
  }

  .user {
    background: #00f0ff;
    color: #000;
    align-self: flex-end;
  }

  .ai {
    background: #9b4cff;
    color: #fff;
    align-self: flex-start;
  }

  .input-container {
    display: flex;
    width: 90%;
    max-width: 600px;
  }

  input {
    flex: 1;
    padding: 10px;
    border-radius: 10px 0 0 10px;
    border: none;
    font-size: 1rem;
  }

  button {
    padding: 10px 20px;
    border: none;
    border-radius: 0 10px 10px 0;
    background: linear-gradient(90deg, #00f0ff, #9b4cff);
    color: #fff;
    cursor: pointer;
    font-weight: bold;
  }

  button:hover {
    opacity: 0.9;
  }
</style>
</head>
<body>

<header>
  <img src="LuNex_logo.png" alt="LuNex Logo">
  <h1>LuNex AI</h1>
</header>

<div class="chat-container" id="chat">
  <!-- Messages will appear here -->
</div>

<div class="input-container">
  <input type="text" id="userInput" placeholder="Ask LuNex something...">
  <button onclick="sendMessage()">Send</button>
</div>

<script>
  const chatContainer = document.getElementById('chat');
  const apiKey = "YOUR_OPENAI_API_KEY"; // Replace with your OpenAI API key

  function addMessage(text, sender) {
    const msgDiv = document.createElement('div');
    msgDiv.classList.add('chat-message', sender);
    msgDiv.textContent = text;
    chatContainer.appendChild(msgDiv);
    chatContainer.scrollTop = chatContainer.scrollHeight;
  }

  async function getAIResponse(prompt) {
    try {
      const response = await fetch("https://api.openai.com/v1/chat/completions", {
        method: "POST",
        headers: {
          "Content-Type": "application/json",
          "Authorization": `Bearer ${apiKey}`
        },
        body: JSON.stringify({
          model: "gpt-3.5-turbo", // You can switch to gpt-4 if you have access
          messages: [{ role: "user", content: prompt }],
          temperature: 0.7,
          max_tokens: 500
        })
      });

      const data = await response.json();
      return data.choices[0].message.content.trim();
    } catch (error) {
      console.error(error);
      return "Oops! Something went wrong. Please try again.";
    }
  }

  async function sendMessage() {
    const input = document.getElementById('userInput');
    const userText = input.value.trim();
    if (!userText) return;

    addMessage(userText, 'user');
    input.value = '';

    addMessage("LuNex is thinking...", 'ai');
    const aiResponse = await getAIResponse(userText);

    // Remove "thinking" message
    const thinkingMsg = chatContainer.querySelector(".ai:last-child");
    thinkingMsg.remove();

    addMessage(aiResponse, 'ai');
  }

  // Press Enter to send
  document.getElementById('userInput').addEventListener('keypress', (e) => {
    if (e.key === 'Enter') sendMessage();
  });
</script>

</body>
</html>

